
const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className="text-3xl text-center font-bold text-[#14274E] py-3 ">{title}</h2>
        </div>
    );
};

export default SectionTitle;